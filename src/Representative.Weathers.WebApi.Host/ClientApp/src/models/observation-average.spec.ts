import { ObservationAverage } from './observation-average';

describe('ObservationAverage', () => {
  it('should create an instance', () => {
    expect(new ObservationAverage()).toBeTruthy();
  });
});
